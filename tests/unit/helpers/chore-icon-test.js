
import { choreIcon } from 'mirror-hud/helpers/chore-icon';
import { module, test } from 'qunit';

module('Unit | Helper | chore icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = choreIcon([42]);
  assert.ok(result);
});

