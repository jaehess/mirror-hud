
import { formatTemperature } from 'mirror-hud/helpers/format-temperature';
import { module, test } from 'qunit';

module('Unit | Helper | format temperature');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTemperature([42]);
  assert.ok(result);
});
